package com.example.sportapp_backend.service;

/*import com.example.sportapp_backend.entity.Course;
import org.junit.jupiter.api.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import java.util.List;

import static org.junit.jupiter.api.Assertions.*;
@RunWith(SpringRunner.class)
@SpringBootTest*/
class CourseServiceTest {

    /*@Autowired
    private CourseService courseService;

    @Test
    void getAllCourses() {
        List<Course> rt = courseService.getAllCourses();
        assertNotNull(rt);
    }

    @Test
    void getCoursesByType() {
        List<Course> rt = courseService.getCoursesByType(1, 1);
        assertEquals(1, rt.get(0).getType1());
    }

    @Test
    void getCoursesByTwoType() {
        List<Course> rt = courseService.getCoursesByType(1, 1);
        assertEquals(1, rt.get(0).getType1());
        assertEquals(111, rt.get(0).getType2());
    }*/
}
