package com.example.sportapp_backend.service;

/*import com.example.sportapp_backend.entity.Target_like;
import org.junit.jupiter.api.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.junit4.SpringRunner;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

import static org.junit.jupiter.api.Assertions.*;
@RunWith(SpringRunner.class)
@SpringBootTest*/
class LikeServiceTest {

    /*@Autowired
    private LikeService likeService;
    @Test
    void addNewLike() {
        Target_like t = likeService.addNewLike(1, 1, 1);
        assertEquals(-1, t.getLike_id());
    }

    @Test
    @Rollback(value = true)
    @Transactional
    void removeLike() {
        int rt = likeService.removeLike(1, 1, 1);
        assertEquals(1, rt);
    }

    @Test
    void getLikeNum() {
        int r = likeService.getLikeNum(1, 1);
        assertEquals(6, r);
    }

    @Test
    void getUserLikes() {
        List<Target_like> rt = likeService.getUserLikes(1);
        assertEquals(1, rt.get(0).getLike_id());
    }

    @Test
    void checkLiked() {
        Target_like rt = likeService.checkLiked(1, 1, 1);
        assertEquals(1, rt.getLike_id());
    }*/
}
