package com.example.sportapp_backend.service;

/*import com.example.sportapp_backend.serviceImpl.MsmServiceImpl;
import org.junit.jupiter.api.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.junit4.SpringRunner;
import org.springframework.transaction.annotation.Transactional;

import static org.junit.jupiter.api.Assertions.*;
@RunWith(SpringRunner.class)
@SpringBootTest*/
class MsmServiceTest {

    /*@Autowired
    private MsmService msmService;

    @MockBean
    private MsmServiceImpl msmServiceImpl;

    @Test
    @Rollback(value = true)
    @Transactional
    void send() {
        boolean r = msmServiceImpl.send("18992440131");
        assertEquals(false, r);
    }

    @Test
    @Rollback(value = true)
    @Transactional
    void checkCode() {
        boolean r = msmServiceImpl.checkCode("18992440131", "123");
        assertEquals(false, r);
    }*/
}
