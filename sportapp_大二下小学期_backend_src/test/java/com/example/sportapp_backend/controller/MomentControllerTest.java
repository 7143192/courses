package com.example.sportapp_backend.controller;

/*import com.example.sportapp_backend.service.LikeService;
import com.example.sportapp_backend.service.MomentService;
import org.junit.jupiter.api.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.context.junit4.SpringRunner;
import org.springframework.test.web.servlet.MockMvc;

import static org.junit.jupiter.api.Assertions.*;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

@RunWith(SpringRunner.class)
@WebMvcTest(MomentController.class)
@AutoConfigureMockMvc*/
class MomentControllerTest {
    /*@Autowired
    private MockMvc mockMvc;

    @MockBean
    private MomentService momentService;

    private String getMethodName() {
        StackTraceElement[] stacktrace = Thread.currentThread().getStackTrace();
        StackTraceElement e = stacktrace[2];
        String methodName = e.getMethodName();
        return methodName;
    }

    @Test
    void getAllMoments() throws Exception {
        this.mockMvc.perform(get(
                "/"+getMethodName())
        ).andExpect(status().isOk());
    }

    @Test
    void getUserMoments() throws Exception {
        this.mockMvc.perform(get(
                "/"+getMethodName())
                .param("userId", "1")
        ).andExpect(status().isOk());
    }

    @Test
    void getFollowMoments() throws Exception {
        this.mockMvc.perform(get(
                "/"+getMethodName())
                .param("userId", "1")
        ).andExpect(status().isOk());

    }

    @Test
    void delMoment() throws Exception {
        this.mockMvc.perform(get(
                "/"+getMethodName())
                .param("momentId", "1")
        ).andExpect(status().isOk());
    }

    @Test
    void addMoment() throws Exception {
        this.mockMvc.perform(get(
                "/"+getMethodName())
                .param("userId", "1")
                .param("content", "test")
        ).andExpect(status().isOk());
    }*/
}
